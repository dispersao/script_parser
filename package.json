{
  "name": "dispersao-parser",
  "description": "parser of disperso script",
  "version": "0.0.0",
  "homepage": "",
  "main": "src",
  "author": {
    "name": "mairasala",
    "email": "mairasala@gmail.com"
  },
  "contributors": [],
  "bugs": {},
  "directories": {
    "lib": "src",
    "test": "test/",
    "config": "config/"
  },
  "engines": {
    "node": "^8.0.0",
    "npm": ">= 3.0.0"
  },
  "scripts": {
    "test": "npm run eslint && NODE_ENV= npm run jest",
    "eslint": "eslint src/. test/. --config .eslintrc.json",
    "dev": "nodemon src/",
    "start": "node src/",
    "startDev": "node --inspect src/",
    "jest": "jest",
    "heroku-postbuild": "cd react-ui && npm install && npm run build",
    "build-ol": "cd react-ui/ && npm install && npm install --only=dev --no-shrinkwrap && npm run build"
  },
  "dependencies": {
    "axios": "^0.19.0",
    "ffmpeg": "0.0.4",
    "lodash": "^4.17.11",
    "node-fetch": "^2.6.0"
  },
  "devDependencies": {
    "eslint": "^5.16.0",
    "jest": "24.7.1"
  }
}
